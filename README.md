# Cryptography
Cryptography Study

Setting up
1. Install python 3
2.  pip install pycryptodome

Symetric

AES: Uses keys of 128bits(16byte) ,192bits(24byte) or 256bits(32byte)
Block size: 16byte/128 bits

1. AES with ECB mode

